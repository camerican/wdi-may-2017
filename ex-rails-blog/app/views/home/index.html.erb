	<h1>Welcome Home!</h1>

	<%= flash[:notice] %>
	<%# to do: only show this if we're not logged in %>
	<% if @current_user %>

		Welcome, <%= @current_user.full_name %>

	<% else %>

	<%= form_tag '/login' do %>
	<%= text_field_tag :email %>
	<%= password_field_tag :password %>
	<%= submit_tag 'Login' %>
	<% end %>
	<% end %>